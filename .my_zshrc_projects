export MT_DIR=~/git/mediatum
export ARG_DIR=~/git/arguments
export MEDIATUM_PLUGIN_NAMES="mediatum-tum mediatum-armus mediatum-survey mediatum-scholar"

alias mt="cd $MT_DIR"
alias arg="cd $ARG_DIR"

export WORKON_HOME=~/coding/Venvs

source ~/.local/bin/virtualenvwrapper.sh 2> /dev/null
