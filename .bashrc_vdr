source /etc/profile

if [ -f ~/.my_bashrc_projects ]; then
    . ~/.my_bashrc_projects
fi

if [ -f ~/.my_bashrc_gentoo ]; then
    . ~/.my_bashrc_gentoo
fi

if [ -f ~/.my_bashrc_common ]; then
    . ~/.my_bashrc_common
fi
